{{flutter_js}}
{{flutter_build_config}}

// This app depends on live Firebase/API data. Do not register Flutter's
// offline service worker, because it can keep an older UI after deployment.
_flutter.loader.load();
